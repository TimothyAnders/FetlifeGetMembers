CREATE TABLE Users(
  Label VARCHAR(255),
  LinkToUser VARCHAR(255), 
  Location VARCHAR(255) PRIMARY KEY NOT NULL,
  Username VARCHAR(255) PRIMARY KEY NOT NULL UNIQUE
);

CREATE TABLE Requested_Users (Username VARCHAR(255) PRIMARY KEY NOT NULL UNIQUE);
